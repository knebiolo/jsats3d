# Data Directory

This folder is reserved for local, non-sensitive data inputs, sample lookup files, or local SQLite staging databases.

Raw source datasets reside on the network share:
`K:\Jobs\5662\001\Data\DataTrans\2025_Data`
